<form method="post" action="{{ path('my_event_schedule_delete', {'id': my_event_schedule.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ my_event_schedule.id) }}">
    <button class="btn">Delete</button>
</form>
